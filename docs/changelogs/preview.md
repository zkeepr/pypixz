# Preview Changelog

## [2.0.0][] - 2025-11-29

### Build and Packaging Improvements

- Added the ability to import exceptions from PyPixz.
- Removing the `get_module_info` function to eliminate the `requests` dependency.
- The `install modules` function has been renamed to `install_package`.
- Removing network exceptions and unused exceptions.
- The `install.py` file has been moved to the `scripts` directory.
- The file `install_packages.py` has been renamed to `install.py`.
- The code has been completely redesigned, optimized and reformatted (too lazy to list the changes).

### Publish Python Package

- Updated information for `pyproject.toml` and `setup.py`.

### Documentation and Help Content

- Moved the changelog directory to the `docs` folder.
- Change to `README.md` and the information provided.
- Authors changed by `ZKeepr`.
- Change of copyright.
- Change to the description of PyPixz.

[2.0.0]: https://github.com/zkeepr/PyPIxz/compare/v1.2.0...v1.2.1
